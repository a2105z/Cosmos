# Deploy Cosmos backend to Render
# https://render.com
services:
  - type: web
    name: cosmos-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    rootDir: backend
